// ตัวอย่างไฟล์ Config - ใช้สำหรับคนอื่นที่ clone โปรเจค
// คัดลอกไฟล์นี้เป็น config.js แล้วใส่ API Key จริง

const CONFIG = {
    GEMINI_API_KEY: 'YOUR_API_KEY_HERE' // ⬅️ แทนที่ด้วย API Key ของคุณ
};

window.CONFIG = CONFIG;
